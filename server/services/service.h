#include "../basic.h"
#include "../login/login_server.h"

void add_record(record_db *record_db);
void search_records(char *column, char *research);
void register_user(user_login *user, user_permissions *permissions);